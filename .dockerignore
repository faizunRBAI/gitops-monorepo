# The build context is ./app (see the build-push stage). Everything the image
# does not need is excluded so context uploads stay small and a stale local
# build never leaks into the image.
target/
.git
.gitignore
.github
.udap
infra
gitops
flux
chart
README.md
**/*.md
.idea
.vscode
.DS_Store
